/**
 * Optional metadata for form fields
 */
export interface FieldMetadataOptions {
  // the class will be identified by this field
  primary?: boolean;
  // assign a special css class to this field
  cssClass?: string;
}
